backends = ["vm", "llvm", "hybrid"]

[phases.check]
result = "fail"
diagnostic_code = "KMAC007"
diagnostic_title = "macro target not in appliesTo"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
