backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM110"
diagnostic_title = "local was already moved"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
