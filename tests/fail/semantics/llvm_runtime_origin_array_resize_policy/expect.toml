backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM101"
diagnostic_title = "unsupported array method"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
