backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM111"
diagnostic_title = "cannot move borrowed value"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
