backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM098"
diagnostic_title = "content value is not a widget"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
