backends = ["vm", "llvm", "hybrid"]

[phases.check]
result = "fail"
diagnostic_code = "KMAC017"
diagnostic_title = "macro output is not an expression"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
