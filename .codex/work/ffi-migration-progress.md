# Native FFI migration — progress

**Goal:** seamless C FFI across all executable backends — call C with `@FFI.Extern` and no `@Native`, routed through Kira's own `HostCapabilities`, uniform on VM / LLVM / hybrid, honest on wasm. This is the first FFI slice; the plan lives in `.codex/tmp/ffi-plan.md`.

**Status as of 2026-07-21 ~04:25:** all seven items landed. Workspace green, independently re-verified: fmt, clippy, `build --workspace`, `nextest --workspace` (1495 passed), wasm32 vm-core check, and the three FFI suites — `backend_parity ffi` (vm/llvm/hybrid byte-identical, single-copy counter 1→2), `end_to_end ffi` (5), `end_to_end ffi_wasm` (2). 112 files changed. **Complete and committed** in two commits, both independently re-verified green (fmt, clippy `-D warnings`, `build --workspace`, `nextest --workspace` 1496 passed, three FFI suites, wasm32 vm-core):
- `2fefbb8 Call C through a seamless foreign function interface` — the milestone (117 files).
- `b64cda7 Zero the bridge reserved bytes at the native seam and split two files` — review fixes.

Adversarial review ran on Opus (Fable was unavailable all window — one 429, then two zero-progress hangs). No critical/high defects: wire contracts, all three marshalling paths, host-capability boundary, unsupported-signature refusals, cross-backend parity, and hybrid single-copy held. Three findings, all handled: (1) LLVM `@Native` seam left the 7 reserved `BridgeValue` bytes uninitialized — fixed by routing both seams through one shared `store_bridge` writer + regression test; (2) `parser/item.rs` and `cli/pipeline.rs` crossed the 700-line ladder — split under it; (3) native `RawPtr` read lacks the width check the Rust seams have — no-op on 64-bit, documented in code for a future 32-bit target.

Known honest gap: wasm `CString` is not proven — a **pre-existing** `kira_rt_str_new` bug (64-bit length vs 32-bit wasm `usize`) traps any wasm string, not FFI-specific. Documented in `docs/ffi.md`; scalar-only wasm proof used; host backends prove `CString`.

## Landed and proven

- **Foreign ABI + loader** (`kira-runtime-abi`, `kira-dynamic-ffi`): `ForeignType` tags 0–13, `BridgeValue::RAW_PTR=9` (16-byte layout unchanged), adapter marker `kira_foreign_adapter_abi_version_1`, transparent-`u32` status 0–4, default `HostCapabilities::call_foreign` returns `NoForeignHost`, typed adapter loader with marker check + signature-keyed cache.
- **Frontend** (`kira-syntax-model`, `kira-parser`, `kira-semantics*`, `kira-diagnostic-messages`): `@FFI.Extern { library; symbol; abi: c; }` + bodyless-`;`, `Type::RawPtr`/`CString`, foreign-callable registry (`Callee::Foreign`), seamless calls, `String→CString` coercion, typed refusals `KPAR048–055` / `KSEM176–186`.
- **IR + bytecode + VM** (`kira-ir`, `kira-bytecode`, `kira-vm-runtime`): `IrProgram.foreign_imports` + `IrCallee::Foreign`; append-only `CALL_FOREIGN=0x45` and KBC1 foreign section after exports (forced empty-export prelude); `Value::RawPtr`; interpreter path builds `ForeignArg`s, calls the host, cleans args on every exit; old modules decode as zero imports. VM path proven with an in-test host.
- **Library resolution** (`kira-native-lib-definition`, `kira-manifest`, `kira-project`, `kira-backend-api`): structured `TargetTriple`, `NativeLibs/*.toml` parser, catalog keyed by interned symbol, exact host/wasm matching, typed errors (undeclared library, missing archive, host-only-for-wasm).
- **LLVM adapters + link** (`kira-llvm-backend`, `kira-native-bridge`): one uniform `extern "C"` adapter per import, exact C ABI (modulo narrowing, sign/zero extension, F32 rounding, C `_Bool`, target-width RawPtr, transient CString alloc/free with interior-NUL status), `link_executable` links C archives, `build_adapter_sidecar`, forced adapter symbols; append-only `kira_rt_cstring_new/free` (`RUNTIME_ABI_VERSION` stays 2). Proven by a real native compile → llvm-ar → link → run test asserting byte-exact output for every supported width, Bool, F32/F64, CString length, RawPtr round-trip.

Supported surface: `Void`, `I8/I16/I32/I64`, `U8/U16/U32/U64`, `Bool`, `F32/F64`, `RawPtr`, `CString` params. Deferred to later slices: CString results, aggregates, callbacks, autobind, dynamic libraries, source compilation.

## Landed (items 6–7)

- **Hybrid** (item 6): KHM1 foreign-import/adapter-symbol section (append-only, old manifests decode as zero imports); adapters bound out of the one loaded dylib — no second `dlopen`; `Session::call_foreign`; bundle validation rejects `.kbc`/`.khm` signature drift. Single-copy proven by a stateful C counter reading 1 then 2 across a runtime-half and native-half call in one process.
- **CLI/build wiring** (item 7): `kira-cli/foreign_libs.rs` resolves each import's archive per target; threaded into native/hybrid/wasm paths; VM builds an adapter sidecar and runs against a native-capable `ForeignHost` in `kira-main` (target-gated so `kira-main` still builds for wasm).
- **Wasm**: matching wasm32-emscripten archive passed to emcc; absent wasm target refused before emcc with a typed diagnostic.
- **Fixtures / docs / proof**: checked-in portable C fixture at `crates/kira-cli/tests/fixtures/ffi/`, `docs/ffi.md`, `examples/ffi`, and the three CLI suites all green.
- **Required item-5 follow-up**: `kira-llvm-backend` `link_hybrid_library` added — item 5 emitted hybrid adapters but never linked the C archives / forced adapter symbols, so hybrid FFI could not work; additive, ABI unchanged.

## Done-bar for completion

`cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo build --workspace`; `cargo nextest run --workspace`; `cargo check -p kira-vm-runtime --target wasm32-unknown-unknown`; plus the three CLI FFI suites green with Kira-produced output. After green: one Fable review pass, route findings to a repair, re-run gates, commit.

## Broader migration context

This FFI slice is one milestone in the larger Project Matter migration. Before it, the package resolver milestone landed (committed as `Resolve package directories through the toolchain`), which let `kirac check` on the editor resolve its 9-package graph and reach real semantics — surfacing **1524 honest diagnostics** across 26 files. Those split into genuine missing language features and cases needing `.kira` changes in Project Matter itself; the largest buckets were parser errors around named-argument syntax (`KPAR020`/`KPAR001`), undefined function/name/type cascades (`KSEM061/060/050`), mutable-`self` methods (`KSEM021`), and unimplemented mutable borrow (`KSEM112`). Seamless FFI was chosen as the next slice because the editor is FFI-heavy (graphics/windowing/input host calls); it does not reduce those 1524 directly but unblocks the host-call surface the editor depends on.

Next slices to attack after FFI commits (rough priority): the named-argument parser gap (collapses the largest diagnostic bucket), mutable-`self`/mutable-borrow execution, then re-run `kirac check` on the editor to re-measure.

**Landed (2026-07-21):** named (labeled) call arguments — `4d60ecb Accept labeled arguments at call sites`. Parser accepts `label: expr` in argument lists; semantics resolves each label against the callee's parameters and binds to positional order before IR, so all backends inherit it; unknown/duplicate/missing labels are typed diagnostics; proven byte-identical on vm/llvm/hybrid. Green (one live_hybrid flake, confirmed passing in isolation at 7.74s vs a 194s parallel-contention timeout).

**Editor error count: 1524 → 592** (−932, 61%). This slice collapsed the whole `KPAR001`/`KPAR020` parser bucket plus its downstream cascades.

Remaining top buckets (from the re-measure): `KSEM061` undefined function (249), `KSEM050` unknown type (148), `KSEM021` cannot assign to immutable `self` (103), `KSEM092` unknown struct (28), `KSEM060` undefined name (17), `KSEM112` mutable borrow not implemented (15).

**Classification (read-only investigation, 2026-07-21):** all 592 are **compiler gaps — no genuine Project Matter `.kira` change is needed.** (`Point`/`Size` are defined in the oracle's Foundation `Types/Maths.kira`, which PM correctly imports; kira-rusty ships only a stub Foundation.) Slices, largest lever first:

- **A1 — flat-package-scope aggregate loading — LANDED** (`4d45eed Load every file of a package into one flat scope`). Bare imports now aggregate every package file; two-phase name registration resolves forward references across sibling files; struct value cycles detected/reported. Green (the two `live_hybrid` failures are the known parallel-load flake — 5 passed isolated). `ComponentStore`/`SpatialBvh`/KiraGraphics public types now resolve.

**Scope correction after A1 — the true surface is much larger than 592.** Because A1 now loads *all* package files (not just root files), the editor's `kirac check` error count went **592 → 14867**. This is not a regression: before A1 the compiler never looked at most of the source. The real remaining work, by origin: kira-graphics 10668, project-matter 1747, kira_ui 1182, ui-foundation 1146, kira-layout 124. Top blocker histogram: `KPAR053` ~7102 — the **deferred FFI annotation family** (`@FFI.Struct`/`@FFI.Callback`/`@FFI.Pointer`/`@FFI.Array`; the FFI slice implemented only `@FFI.Extern`), heavily used in kira-graphics's Metal/Vulkan bindings — then `KSEM182` 2339, `KSEM060` 1694, `KSEM050` 935, `KSEM061` 788, `KSEM021` 633, `KPAR020` 446.

Revised slice priority (each substantial): the fuller `@FFI.*` annotation set (largest, unblocks whole binding files — parse first, then C-layout struct / callback semantics), **Construct 2.0**, scalar-conversion calls A3, mutating `self` A4, the Foundation stdlib port A2, `borrow mut` A5. This is now clearly a multi-slice, multi-session effort, not a handful of quick fixes.

**`@FFI.*` annotation family — LANDED** (`c217f3d`). All six forms parse (KPAR053 bucket eliminated); `@FFI.Alias`/`@FFI.Pointer` become type aliases so native pointer params cross the seam; `@FFI.Struct { layout: c }` is a zero-filled C-layout struct lowered to an ordinary struct of zero literals (no IR change, all backends agree); `@FFI.Array` use and `@FFI.Callback` at the seam are declared but refused with typed not-yet-executable diagnostics. Green (1547 tests). **Editor errors 14867 → 6165** (−8702). New top buckets: KSEM060 1694 (undefined name), KSEM050 1406 (unknown type), KSEM061 775, KSEM021 633, KPAR020 446, KSEM182 254.

**Construct 2.0 — LANDED, executing** (`3c6573e Execute construct-backed declarations on every backend`). A construct-backed `Widget Name(params){ … node … }` compiles as a class-shaped struct — params → stored fields filled by the construction call, `let node: Any { block }` → a zero-arg method (block tail becomes its return), `.node` read → a method call, construction → the existing struct-new node. No new IR/opcodes; runs byte-identical on vm/llvm/hybrid (proven by `backend_parity/constructs.rs`). Child slots / `@Consuming` / `extends`/`requires` refused with typed diagnostics (KSEM200–208, KPAR057–062). Green (1567 tests). Editor errors 6165 → 6877 (+712): construct bodies now parse instead of being one opaque `KSEM900` per file, surfacing genuine gaps beneath. New top buckets: KSEM060 1948 (undefined name), KSEM050 1403 (unknown `Module.Type`), KSEM061 774, KSEM021 633 (mutable self), KPAR020 598 (qualified struct literal `Module.Type {}`), KSEM182 254.

**Qualified cross-module references — LANDED** (`3060cb7 Resolve qualified struct literals and enum variants`). Qualified type refs + calls already worked; added qualified struct literals `Module.Type { … }` (was a parse cascade) and qualified enum variants `Module.Enum.Variant`, both resolving to the flat global tables — no IR/backend change, byte-identical. Generic-enum qualified variants (`Result.Ok`) deferred (no type args to infer). Green (1576 tests). **Editor 6877 → 5051** (−1826). Top buckets now: KSEM050 1403 (FFI C typedefs `VkFlags`/`UINT`/`BOOL` ~1200 + Foundation `Rect`/`Size`/`Point` ~163), KSEM060 873, KSEM061 771 (scalar conversion calls `Int`/`U32`/`U64`/`Float` ~600 + native intrinsics), KSEM021 633 (mutable self), KSEM182 254.

**Scalar conversion calls — LANDED** (`ad45233 Convert between numeric scalar types at a type-name call`). `Target(expr)` for a scalar type `Target` is a numeric conversion: Int/Float → any int/float width; same-kind width changes re-tag, int↔float lower to two append-only opcodes (`CONVERT_INT_TO_FLOAT=0x46`, `CONVERT_FLOAT_TO_INT=0x47`, saturating float→int); Bool excluded per oracle. Byte-identical all backends; widths/arithmetic parity unregressed. Green (1595 tests). **Editor 5051 → 4444** (−607).

**Mutating `self` — LANDED** (`b662164 Let a method mutate its own receiver`). A fixpoint marks each method mutating (incl. transitive `self.child.mutate()`); a mutating method binds `self` as a mutable owned slot and writes the receiver back to the caller's place as a side effect of the call (short-circuit preserved). VM writes back on return via new append-only opcode `CallMut=0x48`; native passes the receiver by pointer. A struct receiver has no seam representation so a mutating call is always same-engine. Green (1612 tests; ownership/classes parity unregressed). **Editor 4444 → 3735** (−709); KSEM021 633→4.

**Struct-field default scope — LANDED** (`194de28 Resolve a field default in its declaring module`). Defaults retain their declaring `SourceId`, resolve once after signatures exist using the defining file's imports + empty local scope, and construction reuses resolved HIR; undefined defaults reported even unused; recursive defaults KSEM213; top-level function values now fill function-typed defaults through the environment-free closure adapter. Applies to structs/classes/construct-backed decls, no IR/backend change. Green (1619 tests). **Editor 3735 → 3264** (−471 exactly).

**Remaining-error classification complete:** all remaining errors are compiler gaps, autobind-blocked, or Foundation stub — no confirmed PM source bug. Roughly ~2100 compiler-fixable, ~1267 autobind, ~252 Foundation. Ordered next: native-state FFI intrinsics (~422), Foundation content (~252), Construct child-slot completion (~338 incl cascades), FFI aggregate seam (~329), borrow/move (~251), parser gaps (~215), closure captures (~55), macro system.

**Native callback-state FFI intrinsics — LANDED** (`31a54f4 Carry opaque native callback state through every backend`). `nativeState`/`nativeUserData`/`nativeRecover<Value>`/`nativeStateFree` as compiler intrinsics over an opaque runtime-owned box; VM routes through new HostCapabilities methods (portable core keeps no store, still wasm-builds); box in host-capable layer + hybrid native half; runtime type-id validation → deterministic traps on wrong-value-type/null/unknown/freed; four opcodes appended after CallMut (0x49–0x4c); byte-identical vm/llvm/hybrid. Oversized files split to hold the ladder. Green (1638 tests). **Editor 3264 → 2895** (−369).

**live_hybrid flake — FIXED** (`aa5115f Gate a live test edit on the watcher, not a sleep`). Root cause: the test fired its edit after a fixed 6s sleep; under parallel load, session startup exceeded 6s so the edit landed before `SourceWatcher::new` took its baseline → change folded into baseline → reload never fired → 187s timeout. Fix: supervisor emits `live.watch.started` right after the baseline (local-only event, no wire change), and the test gates its edit on that event. Race eliminated at source; 4 back-to-back full-suite runs green. Not a flake anymore.

**Foundation geometry + memberwise construction — LANDED** (`6f3a406`). Ported the oracle's `Point`/`Size`/`Rect`/`Vec3`/`Mat4` + the free math fns the corpus calls into bundled Foundation, and built the implicit **memberwise struct constructor** (`Point(x:.., y:..)` / positional) lowering to the same struct-new as a literal — no IR change, all backends agree; typed diagnostics KSEM223–227. Matrix methods / operator overloading deferred (corpus writes math longhand). Green (1652 tests). **Editor 2895 → 2638** (−257). Toolchain rebuilt via `knvm binstall` — Zed LSP now current (see [[kira-lsp-breaks-in-zed]]).

**`Any` enforcement sweep — COMPLETE, GREEN (2026-07-22).** Kira's top type and heterogeneous construct qualifier are now spelled `Any` across diagnostics, docs, `.kira` examples/tests, comments, and generated Kira-facing text. Type-position placeholders now use `Any` or a descriptive name (`Target`, `Element`, `Value`) instead of conventional single-letter spelling; Rust generics remain unchanged. No compiler mechanism changed. Green: focused parser/semantics/CLI suite 863/863, fmt, clippy `-D warnings`, workspace build, full nextest 1662/1662, wasm32 VM-core check.

**Construct child slots — PARSE landed** (`06bf25e Parse construct child slots and name the deferred dispatch`). `body { }`, `some X`/`[some X]`, `@Content` parse (KPAR059 84→4); heterogeneous execution refused with a precise KSEM228 (131) boundary; KSEM203 narrowed to structural-only (48→1). Green (1662 tests). Editor ~2651.

**`Any Construct` heterogeneous dynamic dispatch — LANDED, executing (2026-07-22).** A construct family now becomes a synthesized `Any Family` enum whose variants carry each concrete backed struct; a two-phase registration (empty enum header before structs resolve fields, variants filled after every backed struct id exists) breaks the family↔struct cycle. Concrete widgets **upcast** into the family at any position expecting it (`analyze_expr_expecting` wraps with `EnumNew`); a family value's method call becomes a synthesized **tag-dispatcher** (`Any Family.method$dispatch`) that projects the concrete payload and calls its own method — so all backends run ordinary enum branch + direct call. The `body { … }` shorthand is now a computed family-typed method; `@Consuming function` parses as an executable method; `Any Family` is a first-class written type (`TypeRef::AnyConstruct`, `KSEM237` for a non-family). Struct payloads in enums are now representable: VM already deep-copied them; native/LLVM add append-only `EnumPayloadKind::AGGREGATE=3` + `kira_rt_enum_new_aggregate`/`kira_rt_enum_payload_aggregate` (erased box carrying the concrete type's clone/free leaves — no ABI-version bump, `KiraEnum` layout unchanged). Family methods that mutate `self` or mismatch the family signature are refused (KSEM234–236). Proven byte-identical on vm/llvm/hybrid by `backend_parity/constructs.rs::heterogeneous_family_values_dispatch_on_every_backend` (differently-shaped children in an `[Any Widget]` slot + an `Any Widget` parameter). Green: fmt, clippy `-D warnings`, build --workspace, nextest **1666/1666**, wasm32 VM-core. **Editor 2651 → 2587** (−64): KSEM228 (131) eliminated; genuine child-type mismatches (KSEM232, from `For`/`if` builder-control-flow and `extend` fluent modifiers, both deferred features) and their KSEM097 method-not-found now surface honestly beneath.

Corpus follow-up (uncommitted, in `../kira_ui`): `[any Widget]` → `[Any Widget]` in `WidgetModel.kira`/`KiraUI.kira` for canonical spelling; left as WIP in that separate repo, not committed here.

**`extend` fluent modifiers — LANDED, executing (2026-07-22).** `extend Family { function … }` adds chainable modifier methods to a construct family. `extend` is a contextual keyword — recognized only in item position by the leading identifier's text, checked before the construct-backed form it shares a shape with — so an ordinary name spelled `extend` is unaffected. Unlike a per-variant family method, a modifier has **one shared body** whose receiver is the family value (`self: Any Family`, local 0); it lowers to a single synthesized function reserved up front (so an uncalled modifier is still checked and lowered). A call resolves the same on a family value and on a concrete widget: the concrete receiver upcasts into the family via the existing `EnumNew` wrap, then both take a direct call — no new IR, opcode, or serialized shape, so all backends inherit it. Uniform modifiers are excluded from family conformance (they are not implemented per variant) and from the dispatcher build. Diagnostics: `KSEM238` extend of an unknown family, `KSEM239` modifier colliding with a family method, `KPAR063`/`KPAR064` malformed block. Proven byte-identical on vm/llvm/hybrid by `backend_parity/constructs.rs::extend_modifiers_chain_on_every_backend`. Green: fmt, clippy `-D warnings`, build --workspace, nextest **1673/1673**, wasm32 VM-core. **Editor 2587 → 2531** (−56): KSEM097 method-not-found **77 → 0** (every fluent-modifier call in `kira_ui/app/Modifiers.kira` now resolves).

Discovered blocker (own slice, not this one): 2 of the 19 modifiers — `font`, `surfaceBorder` — use **default parameter values** (`weight: FontWeight = FontWeight.Regular`), which `parse_params` does not accept for *any* function (confirmed on a bare `function f(x: Int, y: Int = 3)`; it is a pre-existing global parser gap, not extend-specific). Those two surface as `KPAR064` until function param defaults land.

**Handoff / working-tree state (for a fresh session after `/clear`):** the last committed slice is `40c389a` (extend fluent modifiers), atop `7b0946f` (Any Construct dispatch) and `33e5e01` (Any spelling sweep). The only other uncommitted files in kira-rusty are `AGENTS.md` and `Cargo.toml` — modified at an earlier session's start, NOT part of any slice; leave them alone (ask the user before touching). Scratch under `.codex/tmp/` is gitignored.

**Next slice (start here on `continue`): function parameter defaults.** No call may omit any argument today — confirmed: `add(1)` on a 2-param `add` is `KSEM062: takes 2 argument(s), found 1`; only labels (`add(a: 1, b: 2)`) and struct/enum/construct *field* defaults exist, never call-arg fill. Parse `name: Type = expr` in `parse_params` (struct/enum/construct fields already do this — `eat(Equals).then(parse_expr)`); then the real work is semantics: a call omitting a defaulted arg fills it from the default expr across every call path (free fn, method, family/uniform method), resolved into HIR args so all backends inherit with no IR change, mirroring struct-field defaults. Unblocks the last 2 modifiers (`font`, `surfaceBorder`, currently `KPAR064`) and the corpus more broadly. Prove parity, run the gates, commit.

After that: builder control-flow (`For`/`if` content items) — the remaining KSEM232 12 — then FFI aggregate seam (~329, KSEM182 254), borrow/move (KSEM112 98), parser gaps, closures, macros. KSEM050 1232 is the autobind-blocked C-typedef set, awaiting the curated-prelude-vs-build-autobind call.

--- (superseded design notes below) ---

**Construct child slots — historical:** The `body { child… }` / `some X` / `[some X]` slot forms now PARSE (KPAR059 84→4). But full child-slot execution for a heterogeneous `[Widget]` slot (holding different construct-backed types) is the **`Any Construct` dynamic-dispatch feature** — a cyclic two-phase registration (family enum ↔ backed-decl structs ↔ family enum) + heterogeneous upcast wrapping + synthesized per-method dispatchers, the oracle's explicitly-deferred later stage, a multi-session slice. This slice lands: parse + honest reclassification (heterogeneous composition → its own precise diagnostic, KSEM203 kept for structural-only) + any simple/homogeneous slot that executes within existing struct machinery. Tree green at 1661 tests. **Full heterogeneous Widget child execution is now its own queued slice ("Any Construct" dynamic dispatch)** — the biggest remaining UI-execution feature.

**FFI C typedefs (KSEM050 ~1200) — NEEDS INVESTIGATION, queued.** `VkFlags`/`UINT`/`BOOL`/`CBool`/`UINT64` are used as `@FFI.Alias` targets (612 aliases in kira-graphics) but the base types themselves are **never defined** in the `.kira` sources — they're the primitive C typedefs (`VkFlags`=uint32) that autobind generates from headers, and autobind is deferred. Not a resolution bug. Next: investigate whether kira-rusty should provide a builtin C-primitive-typedef set (a toolchain-side prelude, not a Project Matter edit) or whether this is genuinely autobind-blocked. Also in KSEM050: ~163 Foundation types (`Rect`/`Size`/`Point`) = the Foundation stdlib port (A2).

**LSP-in-Zed (deferred, not dropped):** reinstall path is `kira-knvm` source-install (rebuild + install the dev toolchain from HEAD). Not running a blind heavy rebuild on the unconfirmed stale-binary hypothesis — will run it once the symptom is confirmed. See [[kira-lsp-breaks-in-zed]].

Prior note (superseded design plan): Opus agent landing a runnable increment: construct-backed `Widget Name(params){ … }` declarations parse, type-check for family conformance, and EXECUTE (construct + read `node` byte-identical on VM/LLVM/hybrid); advanced slot/fluent/`any` bits refused with typed diagnostics. Likely a big share of the current KSEM060/050/061 buckets (the unrecognized `Widget Foo(...)` forms across kira_ui/ui-foundation).

Prior note: PM's UI layer is built on construct-backed declarations — `Widget Name(params) { body }` conforming to `construct Widget`/`construct Panel` (kira_ui, ui-foundation: Rectangle/HGroup/VGroup/TextureViewport/…). kira-rusty only parse-don't-crashes these today, so a large share of kira_ui's 1182 + ui-foundation's 1146 errors are here. The construct declaration-family system (family conformance, `extends`, `requires`, `@Required`, `some X`/`[some X]` slots, KSEM118–167, the terminal-`node` bridge) must land. This slice must EXECUTE, not just validate (user confirmed): construct-backed widget declarations have to run on all four backends with parity — beyond kira-zig's documented validate-only state for the construct family. Scope it as a full frontend-through-backend slice: family conformance + construct-backed declaration lowering + the terminal-`node` bridge, executed and proven byte-identical on VM/LLVM/hybrid. Consult the oracle for the intended runtime behavior of a construct-backed declaration and its `node`, then design execution fresh. (Correcting an earlier wrong call that Construct 2.0 wasn't needed — that was based on grepping the `construct` keyword in too few files and missing the `Widget Foo(...)` construct-backed form.)
- **A2 — port the Foundation stdlib content (~113):** kira-rusty's bundled Foundation ships only `printLine`; the oracle's `Point`/`Size`/`Rect`/`EdgeInsets` etc. are absent. Needs A1 (multi-file Foundation) first.
- **A3 — scalar conversion calls `Int(x)`/`U64(x)`/… (~124):** types resolve but the call form is unrecognized; needs a numeric-cast lowering through IR/VM/LLVM.
- **A4 — mutating methods (`self.field = …`) (103):** `self` is bound immutable; oracle allows struct/class self-mutation. Needs value-semantics writeback proven on all backends.
- **A5 — `borrow mut` (15)** and **A6 — parser/inference misc (~36).**

## How this compares to kira-zig

kira-zig's VM already called C without `@Native` — but via **libffi baked into the VM**, and its LLVM/hybrid backends still **required** `@Native`. This slice is seamless **uniformly** across all backends, carries **no libffi** in the VM (routed through `HostCapabilities` + a Kira-generated adapter sidecar, so `kira-vm-runtime` stays wasm-portable), and drives one adapter ABI across VM/native/hybrid/wasm with byte-identical output required.
