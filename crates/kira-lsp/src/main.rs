//! The Kira language server binary (`kira-language-server`).
//!
//! Speaks LSP over stdio via `lsp-server`, backed by the same salsa frontend
//! the compiler uses. Scaffold: transport and lifecycle land first, analysis
//! wires in with the semantics crates.

fn main() {
    eprintln!("kira-language-server: not yet implemented");
    std::process::exit(2);
}
