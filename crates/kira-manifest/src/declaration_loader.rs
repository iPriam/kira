//! Loads a `ProjectManifest` from a `package.kira` declaration (the `Package`
//! construct parsed with the real Kira frontend).
//!
//! Port target: kira-zig `kira_manifest/src/declaration_loader.zig`.
