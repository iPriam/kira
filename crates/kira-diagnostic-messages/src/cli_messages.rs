//! CLI messages (`KCL*`): command, flag, target, runner, and live-mode failures.
//!
//! Mirrors kira-zig `packages/kira_diagnostic_messages/src/CliMessages.zig`.
//! TODO(port): the message constructors port mechanically during migration,
//! each going through `crate::diagnostic_message::build`.
