//! Package-root to source-root mapping.
//!
//! Ported from kira-zig `kira_program_graph/src/roots.zig`. Owns
//! `sourceRootForPackageRoot` (always selects `<package>/app`) and
//! `canonicalAppSourceRoot`.

// TODO(port): `source_root_for_package_root`, `canonical_app_source_root`.
