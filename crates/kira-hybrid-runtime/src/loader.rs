//! Loads a hybrid module bundle (manifest + bytecode + native library path)
//! from disk into a runnable `HybridRuntime`.
//!
//! Ported from kira-zig `packages/kira_hybrid_runtime/src/loader.zig`.
//! Logic lands with the loader port.
