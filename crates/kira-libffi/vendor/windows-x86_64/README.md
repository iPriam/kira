libffi 3.5.2 Windows x86_64 artifacts reused from kira-swift's checked-in
ClibffiWindows package. The import library and DLL are shipped together; the
runtime loads the DLL from Kira's executable or package directory.
