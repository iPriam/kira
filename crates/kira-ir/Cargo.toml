[package]
name = "kira-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

# Deliberately model-only: this crate sits in the VM's dependency cone, so it
# must never pull the salsa-driven analyzer (portable-core rule). The salsa
# query wiring analysis to lowering lives in the CLI.
[dependencies]
kira-runtime-abi.workspace = true
kira-semantics-model.workspace = true
la-arena.workspace = true

[dev-dependencies]
kira-source.workspace = true
