//! CGU content hashing: stable hashes over IR + flags that key the object
//! cache.
//!
//! Ported from kira-zig `packages/kira_llvm_backend/src/cgu_hash.zig`
//! (tests in `cgu_hash_tests.zig` stay test-side).
