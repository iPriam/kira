name: Provision the native toolchain
description: >
  Unpacks the managed LLVM bundle this repository pins and installs the pinned
  libffi, the two native dependencies every crate that links the compiler needs
  before it will compile. Both workflows that build `kira-cli` need them, and a
  second copy of either would be a second source of truth for a pin that
  llvm-metadata.toml already owns.

inputs:
  host-key:
    description: The llvm-metadata.toml target key for this runner.
    required: true
  token:
    description: The token release assets are downloaded with.
    required: true

runs:
  using: composite
  steps:
    - name: Provision the managed LLVM
      shell: bash
      run: |
        version="$(sed -n 's/^version = "\(.*\)"/\1/p' llvm-metadata.toml | head -1)"
        release="$(sed -n 's/^release_tag = "\(.*\)"/\1/p' llvm-metadata.toml | head -1)"
        # The asset name is read out of the metadata rather than spelled here.
        # That file says nothing invents asset names, and it means it: it
        # carries one archive format per host, and a name assembled from a
        # hardcoded `.tar.xz` is a second source of truth that the Windows
        # bundle — a `.zip` — contradicts on its first run.
        asset="$(awk -v section="[target.${{ inputs.host-key }}]" '
          $0 == section { inside = 1; next }
          /^\[/ { inside = 0 }
          inside && $1 == "asset" { gsub(/^[^"]*"|"$/, ""); print; exit }
        ' llvm-metadata.toml)"
        if [ -z "$asset" ]; then
          echo "llvm-metadata.toml names no asset for ${{ inputs.host-key }}" >&2
          exit 1
        fi
        home="$HOME/.kira/toolchains/llvm/$version/${{ inputs.host-key }}"
        mkdir -p "$home"
        case "$asset" in
          *.zip)
            gh release download "$release" --repo "$GITHUB_REPOSITORY" \
              --pattern "$asset" --output "$RUNNER_TEMP/llvm-bundle.zip"
            unzip -q "$RUNNER_TEMP/llvm-bundle.zip" -d "$home"
            ;;
          *)
            gh release download "$release" --repo "$GITHUB_REPOSITORY" \
              --pattern "$asset" --output - | tar -xJ -C "$home"
            ;;
        esac
        # The pin names the code generators every bundle must carry, and a
        # release owns its assets for good: an asset published before the pin
        # grew one is a real LLVM that cannot emit for the device that
        # generator serves. A run against it proves the wrong toolchain, so
        # it stops here, where one message names the bundle and the fix,
        # rather than in whichever suite reached that device first.
        case "${{ inputs.host-key }}" in
          x86_64-*) host_generator=X86 ;;
          aarch64-*) host_generator=AArch64 ;;
          *)
            echo "no LLVM code generator is known for ${{ inputs.host-key }}" >&2
            exit 1
            ;;
        esac
        targets="$(sed -n 's/^targets_to_build = "\(.*\)"/\1/p' llvm-metadata.toml | head -1)"
        config="$home/bin/llvm-config"
        [ -x "$config" ] || config="$config.exe"
        built="$("$config" --targets-built)"
        missing=""
        for generator in $(printf '%s' "$targets" | tr ';' ' '); do
          [ "$generator" = host ] && generator="$host_generator"
          case " $built " in
            *" $generator "*) ;;
            *) missing="$missing $generator" ;;
          esac
        done
        if [ -n "$missing" ]; then
          echo "the bundles published under $release carry no$missing code generator;" >&2
          echo "llvm-metadata.toml pins \"$targets\" and this one was built with: $built." >&2
          echo "Re-run the LLVM toolchains workflow with publish enabled to replace the assets." >&2
          exit 1
        fi
      env:
        GH_TOKEN: ${{ inputs.token }}

    # libffi is linked into the compiler statically, so the archive for the
    # machine being built for has to exist before anything in the workspace
    # compiles. knvm is the installer that fetches them and does not depend on
    # the crate that links them, so it builds here without the archive it is
    # about to install: this provisions libffi exactly as a user does, through
    # the pin and the checksum verification, rather than through a second copy
    # of the download open-coded in YAML.
    - name: Provision the pinned libffi
      shell: bash
      run: cargo run -p kira-knvm --bin knvm -- install libffi
      env:
        GH_TOKEN: ${{ inputs.token }}
