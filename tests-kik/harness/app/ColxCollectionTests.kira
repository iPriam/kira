import Foundation

// ColxCollectionTests.kira
// Deep array test suite. All top-level names use colx / Colx prefix.
// ~85 tests (including ~20 trap tests).

// ---------------------------------------------------------------------------
// Struct types
// ---------------------------------------------------------------------------

struct ColxPt {
    var x: Int
    var y: Int
}

struct ColxBox {
    var lo: Int
    var hi: Int

    function size() -> Int {
        return self.hi - self.lo
    }

    function mid() -> Int {
        return (self.lo + self.hi) / 2
    }
}

struct ColxGrid {
    var cells: [Int]

    function sum() -> Int {
        var t = 0
        var i = 0
        while i < self.cells.count {
            t = t + self.cells[i]
            i = i + 1
        }
        return t
    }

    function cellCount() -> Int {
        return self.cells.count
    }
}

// ---------------------------------------------------------------------------
// Helper functions
// ---------------------------------------------------------------------------

function colxSum(xs: borrow [Int]) -> Int {
    var total = 0
    var i = 0
    while i < xs.count {
        total = total + xs[i]
        i = i + 1
    }
    return total
}

function colxMin(xs: borrow [Int]) -> Int {
    var m = xs[0]
    var i = 1
    while i < xs.count {
        if xs[i] < m {
            m = xs[i]
        }
        i = i + 1
    }
    return m
}

function colxMax(xs: borrow [Int]) -> Int {
    var m = xs[0]
    var i = 1
    while i < xs.count {
        if xs[i] > m {
            m = xs[i]
        }
        i = i + 1
    }
    return m
}

function colxContainsVal(xs: borrow [Int], val: Int) -> Bool {
    var i = 0
    while i < xs.count {
        if xs[i] == val {
            return true
        }
        i = i + 1
    }
    return false
}

function colxBuildRange(n: Int) -> [Int] {
    var xs: [Int] = []
    var i = 0
    while i < n {
        xs.append(i)
        i = i + 1
    }
    return xs
}

function colxBuildSquares(n: Int) -> [Int] {
    var xs: [Int] = []
    var i = 0
    while i < n {
        xs.append(i * i)
        i = i + 1
    }
    return xs
}

function colxBuildFib(n: Int) -> [Int] {
    var xs: [Int] = []
    if n == 0 {
        return xs
    }
    xs.append(0)
    if n == 1 {
        return xs
    }
    xs.append(1)
    var i = 2
    while i < n {
        let p1 = xs[i - 1]
        let p2 = xs[i - 2]
        xs.append(p1 + p2)
        i = i + 1
    }
    return xs
}

function colxReverse(xs: borrow [Int]) -> [Int] {
    var out: [Int] = []
    var i = xs.count
    while i > 0 {
        i = i - 1
        out.append(xs[i])
    }
    return out
}

function colxCountGt(xs: borrow [Int], thresh: Int) -> Int {
    var c = 0
    var i = 0
    while i < xs.count {
        if xs[i] > thresh {
            c = c + 1
        }
        i = i + 1
    }
    return c
}

function colxProduct(xs: borrow [Int]) -> Int {
    var p = 1
    var i = 0
    while i < xs.count {
        p = p * xs[i]
        i = i + 1
    }
    return p
}

// ---------------------------------------------------------------------------
// GROUP A - Empty / count
// ---------------------------------------------------------------------------

construct ColxEmptyCount() extends Test {
    test {
        let xs: [Int] = []
        return xs.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

construct ColxOneAppendCount() extends Test {
    test {
        var xs: [Int] = []
        xs.append(42)
        return xs.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(1); return e }
}

construct ColxFiveAppendCount() extends Test {
    test {
        var xs: [Int] = []
        xs.append(1)
        xs.append(2)
        xs.append(3)
        xs.append(4)
        xs.append(5)
        return xs.count
    }
    expect { let e: Result<Int, TestFailure> = .Ok(5); return e }
}

construct ColxTrapEmptyIdx0() extends Test {
    test {
        var xs: [Int] = []
        let _ = xs[0]
        return 0
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

construct ColxTrapEmptyIdx99() extends Test {
    test {
        var xs: [Int] = []
        let _ = xs[99]
        return 0
    }
    expect { let e: Result<Int, TestFailure> = .Error(.Runtime("")); return e }
}

// ---------------------------------------------------------------------------
// GROUP B - Sums via helpers
// ---------------------------------------------------------------------------

construct ColxSumLiteral() extends Test {
    test {
        var xs: [Int] = []
        xs.append(1)
        xs.append(2)
        xs.append(3)
        xs.append(4)
        xs.append(5)
        return colxSum(xs)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(15); return e }
}

construct ColxSumRange10() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxSum(xs)
    }
    // 0+1+...+9 = 45
    expect { let e: Result<Int, TestFailure> = .Ok(45); return e }
}

construct ColxSumSquares5() extends Test {
    test {
        let xs = colxBuildSquares(5)
        return colxSum(xs)
    }
    // 0+1+4+9+16 = 30
    expect { let e: Result<Int, TestFailure> = .Ok(30); return e }
}

construct ColxSumSquares10() extends Test {
    test {
        let xs = colxBuildSquares(10)
        return colxSum(xs)
    }
    // 0+1+4+9+16+25+36+49+64+81 = 285
    expect { let e: Result<Int, TestFailure> = .Ok(285); return e }
}

construct ColxSumFib8() extends Test {
    test {
        let xs = colxBuildFib(8)
        return colxSum(xs)
    }
    // [0,1,1,2,3,5,8,13] -> 33
    expect { let e: Result<Int, TestFailure> = .Ok(33); return e }
}

construct ColxForInSumLiteral() extends Test {
    test {
        var acc = 0
        for v in [10, 20, 30, 40, 50] {
            acc = acc + v
        }
        return acc
    }
    // 10+20+30+40+50 = 150
    expect { let e: Result<Int, TestFailure> = .Ok(150); return e }
}

construct ColxForInProductLiteral() extends Test {
    test {
        var p = 1
        for v in [1, 2, 3, 4, 5] {
            p = p * v
        }
        return p
    }
    // 1*2*3*4*5 = 120
    expect { let e: Result<Int, TestFailure> = .Ok(120); return e }
}

construct ColxForInCountGt4() extends Test {
    test {
        var c = 0
        for v in [3, 1, 4, 1, 5, 9, 2, 6, 5, 3] {
            if v > 4 {
                c = c + 1
            }
        }
        return c
    }
    // elements >4: 5,9,6,5 => 4
    expect { let e: Result<Int, TestFailure> = .Ok(4); return e }
}

construct ColxForInMax() extends Test {
    test {
        var m = 0
        for v in [3, 1, 4, 1, 5, 9, 2, 6, 5, 3] {
            if v > m {
                m = v
            }
        }
        return m
    }
    expect { let e: Result<Int, TestFailure> = .Ok(9); return e }
}

construct ColxForInSquaresSum() extends Test {
    test {
        var acc = 0
        for v in [1, 2, 3, 4, 5] {
            acc = acc + v * v
        }
        return acc
    }
    // 1+4+9+16+25 = 55
    expect { let e: Result<Int, TestFailure> = .Ok(55); return e }
}

// ---------------------------------------------------------------------------
// GROUP C - Min / Max
// ---------------------------------------------------------------------------

construct ColxMinManual5() extends Test {
    test {
        var xs: [Int] = []
        xs.append(5)
        xs.append(3)
        xs.append(8)
        xs.append(2)
        xs.append(7)
        return colxMin(xs)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(2); return e }
}

construct ColxMaxManual5() extends Test {
    test {
        var xs: [Int] = []
        xs.append(5)
        xs.append(3)
        xs.append(8)
        xs.append(2)
        xs.append(7)
        return colxMax(xs)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(8); return e }
}

construct ColxMinAppend5() extends Test {
    test {
        var xs: [Int] = []
        xs.append(3)
        xs.append(1)
        xs.append(4)
        xs.append(1)
        xs.append(5)
        return colxMin(xs)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(1); return e }
}

construct ColxMaxRange10() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxMax(xs)
    }
    expect { let e: Result<Int, TestFailure> = .Ok(9); return e }
}

construct ColxMinSquares8() extends Test {
    test {
        let xs = colxBuildSquares(8)
        return colxMin(xs)
    }
    // squares[0]=0
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

construct ColxMaxSquares5() extends Test {
    test {
        let xs = colxBuildSquares(5)
        return colxMax(xs)
    }
    // [0,1,4,9,16] -> max=16
    expect { let e: Result<Int, TestFailure> = .Ok(16); return e }
}

construct ColxMinFib8() extends Test {
    test {
        let xs = colxBuildFib(8)
        return colxMin(xs)
    }
    // [0,1,1,2,3,5,8,13] -> min=0
    expect { let e: Result<Int, TestFailure> = .Ok(0); return e }
}

construct ColxMaxFib8() extends Test {
    test {
        let xs = colxBuildFib(8)
        return colxMax(xs)
    }
    // max=13
    expect { let e: Result<Int, TestFailure> = .Ok(13); return e }
}

// ---------------------------------------------------------------------------
// GROUP D - Contains
// ---------------------------------------------------------------------------

construct ColxContainsTrue() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxContainsVal(xs, 5)
    }
    expect { let e: Result<Bool, TestFailure> = .Ok(true); return e }
}

construct ColxContainsFalse() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxContainsVal(xs, 10)
    }
    expect { let e: Result<Bool, TestFailure> = .Ok(false); return e }
}

construct ColxContainsFirst() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxContainsVal(xs, 0)
    }
    expect { let e: Result<Bool, TestFailure> = .Ok(true); return e }
}

construct ColxContainsLast() extends Test {
    test {
        let xs = colxBuildRange(10)
        return colxContainsVal(xs, 9)
    }
    expect { let e: Result<Bool, TestFailure> = .Ok(true); return e }
}

construct ColxContainsMissing() extends Test {
    test {
        var xs: [Int] = []
        xs.append(2)
        xs.append(4)
        xs.append(6)
        xs.append(8)
        return colxContainsVal(xs, 5)
    }
    expect { let e: Result<Bool, TestFailure> = .Ok(false); return e }
}

